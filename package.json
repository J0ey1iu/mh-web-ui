{
  "name": "mh-application-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vue-tsc -b && vite build && npm run zip",
    "build:standalone": "vue-tsc -b && vite build --mode development && npm run zip",
    "zip": "node scripts/zip-dist.cjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "marked": "^18.0.3",
    "pinia": "^3.0.4",
    "vue": "^3.5.13",
    "vue-router": "^4.6.4",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.3",
    "bestzip": "^2.2.1",
    "typescript": "~5.7.3",
    "vite": "^6.3.0",
    "vue-tsc": "^2.2.8"
  }
}
